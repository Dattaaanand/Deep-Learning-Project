## Installation

####
If you want to use CPU only, then `pip install torch`

If you have a GPU, then use `pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121`

`pip install pandas`

`pip install numpy`